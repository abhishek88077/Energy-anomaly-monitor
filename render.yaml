services:
  - type: web
    name: energy-anomaly-monitor
    runtime: python
    buildCommand: python3 -m pip install -r requirements.txt
    startCommand: python3 -m gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 180 app:app
    healthCheckPath: /api/health
    autoDeploy: true