services:
  - type: web
    name: energy-anomaly-monitor
    runtime: python
    buildCommand: python3 -m pip install --target .render_packages -r requirements.txt
    startCommand: PYTHONPATH=.render_packages python3 app.py
    healthCheckPath: /api/health
    autoDeploy: true