services:
  - type: web
    name: energy-anomaly-monitor
    runtime: python
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python -m gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 180 app:app
    healthCheckPath: /api/health
    autoDeploy: true